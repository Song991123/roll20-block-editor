/**
 * Generic selectors captured by the Roll20 frame probe and local candidates.
 * Keep this contract source-agnostic: it describes layout families, not a
 * particular sheet or creator.
 */
export const ROLL20_LAYOUT_SELECTORS = Object.freeze([
  '.sheet-2colrow',
  '.sheet-3colrow',
  '.sheet-col',
  'table',
  'thead',
  'tbody',
  'tr',
  'td',
  'th',
  'input',
  'textarea',
  'select',
  "button[type='roll']",
]);
