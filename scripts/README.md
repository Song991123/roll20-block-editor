# Script Index

Date: 2026-06-14

Run scripts from the project root unless noted otherwise.

## Common Commands

| Command | Purpose |
| --- | --- |
| `corepack pnpm run lint` | Lint the app. |
| `corepack pnpm run build` | Production build/type check. |
| `corepack pnpm run guard:ui-copy` | Scan product-facing UI source for mojibake-looking Korean copy regressions. Excludes Roll20 base CSS, fixtures, reports, and imported sheet corpora because user sheets can contain arbitrary languages. |
| `corepack pnpm run check:server-hygiene` | Check for leftover project dev/smoke listeners on `3000`, `3001`, `3002`, and `4300-4499`. Roll20 CDP `9222` is reported as preserved, not a failure. Add `-- --kill-project` only when you intentionally want to stop matching `node.exe` listeners; non-node system/user processes are never killed by this helper. |
| `corepack pnpm run verify:runtime-visibility` | Run the local worker/rolltemplate visibility bundle: worker workspace separation, worker state smoke, Sandbox expected-preview runtime stripping, preview/edit visible-runtime-node checks, and local rolltemplate chat smoke. This is local evidence, not actual Roll20 visual parity. |
| `node scripts/visual_reference_inventory.mjs` | Inventory visual reference candidates. |
| `node scripts/prepare_visual_fixture.mjs <inventory_json> <out_root> <selector>` | Copy selected source sheets into workspace-owned visual fixtures. |
| `node scripts/prepare_explicit_fixture.mjs <fixture_root> <fixture_id> --html <path> --css <path> [--i18n <path>]` | Copy an explicit sheet into a fixture. |
| `node scripts/render_visual_fixture_doc.mjs` | Render prepared visual fixtures into standalone preview HTML. |
| `node scripts/make_visual_diff_pages.mjs` | Build visual diff pages and diagnostics. |
| `corepack pnpm run capture:visual-fixtures -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/visual-fixture-render [--only <fixtureId>]` | Capture live local preview PNGs and write resource diagnostics separately from render status. Local preview capture only; not actual Roll20 visual parity. Needs `corepack pnpm run build` first. |
| `corepack pnpm run capture:visual-fixtures:strict -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/visual-fixture-render [--only <fixtureId>]` | Same local preview capture, but fails when browser-rendered external image/font resources fail. Use this for visual-parity work so missing assets do not hide behind a screenshot PASS. |
| `corepack pnpm run diff:visual-fixtures` | Capture live local preview PNGs, apply `reports/visual-state-candidates/visual-state-candidates-state-map.json` when present, generate visual diff pages, run them in headless Chromium, collect `visual-fixture-diff-results.md/.json`, and write a heuristic cause classification report with state selector/default input/dimension clues. This automates reference-image diagnostics but is still not a Roll20 visual parity gate. Needs `corepack pnpm run build` first. |
| `corepack pnpm run classify:visual-fixtures -- reports/visual-fixture-diff test-fixtures/visual` | Rebuild only the visual diff cause classification from an existing diff result, including state selector/default input/dimension clues. Writes ignored `visual-fixture-diff-classification.md/.json`. |
| `corepack pnpm run smoke:visual-state-candidates -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/visual-state-candidates` | Import visual fixtures, capture initial plus visible `button[type=action]` and checkbox/radio control state candidates, and compare each screenshot to the copied reference image. Writes a compact ignored state map that downstream diff/baseline scripts can reuse. Local state/crop triage only; not actual Roll20 parity. Needs `corepack pnpm run build` first. |
| `node scripts/make_cascade_leak_pages.mjs` | Build browser-computed CSS cascade diagnostic pages from rendered fixture HTML. |
| `node scripts/live_shadow_cascade_smoke.mjs --out-dir ./out --report-dir reports/live-shadow-cascade [--only <fixtureId>]` | Live static app smoke for preview/edit Shadow DOM CSS winners after real browser import. Needs `corepack pnpm run build` first. |
| `corepack pnpm run smoke:preview-edit-visual -- --out-dir ./out --report-dir reports/preview-edit-visual [--only <fixtureId>]` | Live static app screenshot smoke comparing imported fixture preview root vs edit root. Writes local-only screenshots, pixel diagnostics, edit height checks, and DOM signature parity checks; not a Roll20 parity gate. Needs `corepack pnpm run build` first. |
| `corepack pnpm run smoke:imported-edit-sync -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/imported-edit-sync [--only <fixtureId>]` | Live static app smoke for imported real-fixture edit drag: tries visible imported nodes, verifies edit and preview land on the same block position, and checks emitted HTML/CSS position data. Interaction status and resource status are reported separately. Not a Roll20 parity gate. Needs `corepack pnpm run build` first. |
| `corepack pnpm run smoke:imported-edit-sync:strict -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/imported-edit-sync [--only <fixtureId>]` | Same imported edit-sync smoke, but fails when browser-rendered external image/font resources fail. Use this for visual-parity work so asset failures do not hide behind an interaction PASS. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke [--only <fixtureId>]` | Browser smoke for preview iframe roll button -> ChatPane rolltemplate rendering. Writes local-only chat screenshots/results; not actual Roll20 chat parity. Needs `corepack pnpm run build` first. |
| `corepack pnpm run diagnose:roll20-chat-parity -- reports/roll20-actual-compare/<label> [local-chat-screenshot-dir] [fixture-id] [--out-dir <writable-dir>]` | Compare local ChatPane screenshots against actual Roll20 chat screenshots and write parity diagnostics. Use `--out-dir` when the canonical report output is locked or when another script needs isolated candidate probes. Diagnostic only, not visual parity. |
| `corepack pnpm run diagnose:roll20-chat-candidates -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--include-candidates <name[,name]>]` | Runs chat screenshot candidate comparisons. With `--out-dir`, each candidate's internal parity diagnostic writes to an isolated temp subfolder so the canonical `chat-parity-diagnostics` report is not overwritten by experimental screenshots. Use `--include-candidates` to run only the default baseline plus named candidates for faster isolated experiments; included names that are not in the static candidate table are accepted when a matching `--candidate-screenshots name=dir` override is supplied. Diagnostic only, not production CSS. |
| `corepack pnpm run gate:roll20-chat-candidate-experiment -- reports/roll20-actual-compare/<label> --candidate <name> --candidate-smoke <rolltemplate-chat-smoke-results.json> --candidate-screenshots <screenshots-dir> [--source-context-dir <report-dir>] [--out-dir <writable-dir>]` | Bundles one already-generated local ChatPane candidate through candidate comparison, row-raster comparison, style proof, table-width budget, and the final renderer action gate. Candidate names can be dynamic when smoke/screenshots are supplied; style proof remains explicit and currently knows the `yshy-coc-table-source-context*` family, including `*actual-font*` candidates that must use the actual-font YSHY proof instead of the missing-Bookk proof. It does not start a browser/dev server or promote product CSS; use it to keep ignored temp candidate evidence reproducible and wired into the top gate before renderer work continues. |
| `corepack pnpm run plan:roll20-chat-renderer-targets -- reports\roll20-actual-compare\2026-06-18-state-map-v1 [--out-dir <writable-dir>] [--table-budget-dir <report-dir>] [--candidate-comparison-dir <report-dir>] [--source-context-dir <report-dir>]` | Diagnostic-only Roll20 chat renderer target plan. It reads the current actual evidence reports and splits AW2E/YSHY into narrow next experiments so agents do not promote a global ChatPane CSS patch while fixture axes conflict. It also consumes table-budget, candidate-comparison, and source-context evidence, recording table-width budget, rejected candidate, rule-order, font-face, and table-context blockers before renderer CSS review. Use `--out-dir` when the canonical report output files are locked but the evidence folder is still readable; when the canonical source-context report is missing or weak, the plan auto-uses the newest matching ignored temp source-context report for the same run. |
| `corepack pnpm run gate:roll20-chat-template-scope -- reports\roll20-actual-compare\<label> [--out-dir <writable-dir>] [--source-context-dir <report-dir>] [--cell-allocation-dir <report-dir>]` | Diagnostic-only Roll20 chat template-scope gate. It blocks global ChatPane renderer CSS when high-mismatch fixtures require different rolltemplate scopes/models, source/context proof, asset relink blockers, row-raster candidate regressions, or cell-allocation-breaking candidate evidence, and lists the template-scoped next action for each fixture. Use `--out-dir` and report overrides when canonical output files are locked. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-shell-typography --chat-typography-policy roll20-shell-typography` | Diagnostic-only variant that applies observed Roll20 shell typography to local rolltemplate roots/tables. Use only for candidate comparison; it is not production renderer behavior by itself. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-template-typography --chat-typography-policy roll20-template-typography` | Diagnostic-only variant that pushes observed Roll20 template typography/color/letter-spacing onto the rolltemplate root/table/cells. This is a negative-control candidate unless actual candidate comparison proves otherwise; it is not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-cell-metrics --chat-typography-policy roll20-cell-metrics` | Diagnostic-only cell metric variant. It keeps user template font-family intact while testing Roll20-like rolltemplate font-size/letter-spacing/cell allocation; not production renderer behavior by itself. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-roll20-chat-shell-width-340 --chat-geometry-policy roll20-chat-shell-width-340` | Diagnostic-only Roll20 chat shell width variant. It sets the local ChatPane rolltemplate card/message shell to the observed `340px` chat width so agents can test whether message/content width, not template CSS, explains root-width mismatches. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-message-full-width --chat-geometry-policy aw2e-message-full-width` | Diagnostic-only AW2E message-shell variant. It uses the Roll20-like full-width message box only for chat cards containing `.sheet-rolltemplate-aw`, avoiding the fixture-regressing global `340px` shell candidate. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-root-width-actual --chat-geometry-policy aw2e-root-width-actual` | Diagnostic-only AW2E root-width variant. It sets `.sheet-rolltemplate-aw` to the measured actual Roll20 root width so agents can test whether template root intrinsic sizing, not transform, explains the mismatch. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-font-size-only --chat-typography-policy aw2e-font-size-only` | Diagnostic-only AW2E isolation variant. It applies only the actual Roll20-observed `13.65px` font size to `.sheet-rolltemplate-aw` table/cells, separating font size from letter-spacing and broader typography. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-message-width-font-size --chat-geometry-policy aw2e-message-full-width --chat-typography-policy aw2e-font-size-only` | Diagnostic-only AW2E combined width/text-metric variant. It tests the current root-cause hypothesis that AW2E needs Roll20's full-width chat message/content box plus the observed `13.65px` table font size. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-message-width-text-metrics --chat-geometry-policy aw2e-message-full-width --chat-typography-policy aw2e-text-metrics` | Diagnostic-only AW2E combined width/exact text-metrics variant. It tests the narrower hypothesis that Roll20's AW2E message/content width and table/cell text metrics must be modeled together. Current evidence improves AW2E raw crop mismatch but is not a production renderer patch. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-message-source-context --chat-geometry-policy aw2e-message-full-width --chat-typography-policy aw2e-message-cell-wrap-context` | Diagnostic-only AW2E source-context variant. It combines the AW2E full-width message shell with Roll20-observed AW2E cell font/wrap context so candidate comparison, row-raster comparison, and style proof can test the source-context hypothesis as one scoped model. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-aw2e-text-metrics --chat-typography-policy aw2e-text-metrics` | Diagnostic-only AW2E text metric isolation variant. It applies the actual Roll20-observed `13.65px` table/cell text metrics only to `.sheet-rolltemplate-aw`, matching the current AW2E text-width split probe without touching other templates. Not production renderer behavior by itself. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-yshy-bookk-unavailable --chat-font-policy yshy-bookk-unavailable` | Diagnostic-only YSHY/CoC font availability variant. It removes uploaded `BookkMyungjo-Bd` `@font-face` rules from ChatPane rolltemplate CSS. Current evidence shows the local font still remains available and the candidate regresses YSHY; do not promote. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-roll20-sandbox-font-proxy --chat-font-policy roll20-sandbox-font-proxy` | Diagnostic-only Roll20 sandbox font URL variant. It suppresses document-level user font registration and rewrites rolltemplate font URLs through the Roll20 image proxy approximation to test whether actual Roll20 font-face failures are caused by sandbox URL handling. Do not promote without candidate comparison, style proof, row-raster, and renderer gate evidence. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-yshy-table-font-context --chat-typography-policy yshy-table-font-context` | Diagnostic-only YSHY/CoC table font-context variant. It applies the Roll20-observed Proxima table/root context without changing cells. Current evidence regresses YSHY; do not promote. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-yshy-bookk-missing-render --chat-typography-policy yshy-bookk-missing-render` | Diagnostic-only YSHY/CoC missing-font raster variant. It forces Bookk-rendered cells through a deliberately missing family to test fallback glyph sizing. Current evidence widens the YSHY table and regresses pixels; do not promote. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-yshy-sanitize-typography --chat-typography-policy yshy-sanitize-typography` | Diagnostic-only YSHY/CoC sanitize typography variant. It applies actual Roll20-observed typography, wrapping, and border-spacing values only to `.sheet-rolltemplate-coc` to test the table-wide constraint model. Not production renderer behavior by itself. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-table-scale-x --chat-geometry-policy coc-table-scale-x` | Diagnostic-only per-template width variant for `.sheet-rolltemplate-coc` overflowed tables. Use with `diagnose:roll20-chat-candidates`; not production renderer behavior unless actual Roll20 style proof and renderer policy later allow it. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-table-actual-width --chat-geometry-policy coc-table-actual-width` | Diagnostic-only CoC/YSHY measured table-width variant. It forces the local table to the observed actual Roll20 width without transform to test whether width alone explains the screenshot. Not production renderer behavior. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-crop-origin-y20 --chat-geometry-policy coc-crop-origin-y20` | Diagnostic-only CoC/YSHY crop-origin variant. It shifts the local table down to test whether the actual Roll20 chat crop/origin explains YSHY pixels. Current evidence says no meaningful gain; keep it diagnostic-only. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-overflow-crop-model --chat-geometry-policy coc-overflow-crop-model` | Diagnostic-only CoC/YSHY overflow/crop variant inserted after user rolltemplate CSS. It verifies whether a post-user CSS override can reduce the observed YSHY table/crop mismatch. Current evidence says width declarations still do not change the used table width; keep it diagnostic-only. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-overflow-crop-origin-y20 --chat-geometry-policy coc-overflow-crop-origin-y20` | Diagnostic-only CoC/YSHY combined crop-origin variant. It combines measured table width declarations, overflow crop, and a `20px` top-origin offset. Current evidence rejects it with the same row-raster/style signature as actual-font source context; use it only as negative evidence before table intrinsic/max-content work. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-paint-dim-brightness --chat-paint-policy roll20-dim-brightness` / `--chat-paint-policy roll20-dim-saturate` | Diagnostic-only paint decomposition for the YSHY `paint-dim-background` candidate. Use with `diagnose:roll20-chat-candidates` and `diagnose:roll20-chat-candidate-style`; actual Roll20 sidecars must include computed `filter` fields before paint candidates can be style-proven. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-table-intrinsic-clamp --chat-geometry-policy coc-table-intrinsic-clamp` | Diagnostic-only CoC/YSHY table intrinsic clamp variant. It tests a transform-free table width clamp around the measured actual Roll20 scroll width and records computed `min-width`/`max-width` in the sidecar. Do not promote without candidate comparison and style proof. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-intrinsic-spacing --chat-geometry-policy roll20-intrinsic-spacing` | Diagnostic-only intrinsic metric variant that tests Roll20-like rolltemplate `border-spacing: 0` plus `letter-spacing: normal`. Current evidence rejects it as a production candidate because it regresses YSHY; keep it diagnostic-only. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-border-spacing --chat-geometry-policy roll20-border-spacing` | Diagnostic-only border-spacing isolation variant. Current evidence shows no meaningful pixel gain, so do not promote it. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-letter-spacing --chat-geometry-policy roll20-letter-spacing` | Diagnostic-only letter-spacing isolation variant. Current evidence regresses YSHY, so do not promote it. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-paint-dim-background --chat-paint-policy roll20-dim-background` | Diagnostic-only paint variant for Roll20 chat residuals. It slightly dims rolltemplate table/caption/cell paint to test bright-background mismatch; do not expose it as a product setting. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-paint-edge-shadow --chat-paint-policy roll20-edge-shadow` | Diagnostic-only paint variant for Roll20 chat residuals. It adds narrow inset table/cell edge shadows to test border/shadow raster mismatch; do not expose it as a product setting. |
| `node scripts/rolltemplate_chat_smoke.mjs --out-dir ./out --report-dir reports/rolltemplate-chat-smoke-coc-background-size-actual --chat-paint-policy coc-background-size-actual` | Diagnostic-only CoC/YSHY background raster variant. It keeps filters off and tests whether using the actual Roll20 table-width background raster scale explains row-luma mismatch. Do not promote without candidate comparison and style proof. |
| `corepack pnpm run diagnose:roll20-chat-mask-strategy -- reports/roll20-actual-compare/<label>` | Reads chat parity/residual/candidate reports and classifies the next safe renderer strategy from row-band, left-edge, luma, and mask evidence. Diagnostic only; it tells agents when to stop testing broad paint CSS and inspect crop/shell/width models instead. |
| `corepack pnpm run diagnose:roll20-chat-row-paint-source -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>]` | Route stubborn chat row/paint/source mismatches after width/font candidates fail. For YSHY/CoC, `SANITIZE_REPLAY_REJECTED_SOURCE_MODEL_REQUIRED` means replaying observed sanitized typography as local CSS made the fixture worse, so the next work is actual rule order, font-face activation, and table intrinsic source context. Diagnostic only; not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-source-context -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--row-paint-source-dir <dir>] [--intrinsic-width-dir <dir>] [--fixtures-dir <dir>] [--actual-sidecar <fixture-id>=<json>]` | Fuse actual Roll20 chat CSS activation, source fixture rolltemplate CSS declarations, font-face checks, computed table styles, text measurement, and intrinsic width reports before a renderer change. Use `--actual-sidecar` when a fresh Roll20 chat capture lives in ignored temp output instead of the canonical report folder. Use it after YSHY/CoC sanitize replay is rejected to distinguish rule-order/font-face/table-context work from unsafe broad typography/filter/transform CSS. Diagnostic only; not production CSS or visual parity. |
| `corepack pnpm run diagnose:roll20-chat-shell-geometry -- reports/roll20-actual-compare/<label>` | Compares local ChatPane rolltemplate root/table/cell geometry against actual Roll20 chat sidecars. Use after mask strategy says crop/shell/width needs investigation; diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-font-cell -- reports/roll20-actual-compare/<label>` | Classifies whether chat mismatch is a narrow font/cell allocation model, broad typography candidate, or table-width model problem. Diagnostic only; broad typography remains blocked unless actual pixel/style evidence proves it. |
| `corepack pnpm run diagnose:roll20-chat-width -- reports/roll20-actual-compare/<label>` | Builds a per-template chat width model from local ChatPane sidecars and actual Roll20 chat DOM evidence. Use before changing ChatPane width, padding, or overflow behavior; it separates shell/message width, table intrinsic width, and overflowed table crop cases. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-message-shell -- reports/roll20-actual-compare/<label>` | Separates Roll20 chat message-box geometry from rolltemplate template/table geometry. It reports message width, content/template width, chat-right gutter, and actual shell mode (`FULL_CHAT_WIDTH_MESSAGE` or `INSET_CHAT_WIDTH_MESSAGE`). Use before changing message/card width; diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-table-width-budget -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>]` | Builds a table-width delta budget from width, intrinsic, font/glyph, message-shell, candidate, style-proof, and parity reports. Use it to decide whether the next candidate should target message/content width, exact text metrics, or table-layout/intrinsic constraints. Use `--out-dir` for ignored temp evidence when testing candidate inputs without rewriting canonical reports. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-table-intrinsic-probe -- reports/roll20-actual-compare/<label> [local-smoke-json] [--out-dir <writable-dir>] [--actual-sidecar <fixture-id>=<json>]` | Compares local ChatPane DOM metrics against actual Roll20 chat DOM sidecars for root/table/caption/first-cell width, scrollWidth, style deltas, row uniformity, and crop/top-offset signals. Use after table-width budget routes a fixture to table-layout/intrinsic constraints. Use `--actual-sidecar` for fresh ignored-temp Roll20 sidecars and `--out-dir` for ignored temp evidence when testing candidate smokes without rewriting canonical reports. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-table-layout-constraint -- reports/roll20-actual-compare/<label> [local-smoke-json] [--source-context-dir <dir>] [--intrinsic-width-dir <dir>] [--table-intrinsic-dir <dir>] [--actual-sidecar <fixture-id>=<json>] [--out-dir <dir>]` | Checks whether `width`/`min-width`/`max-width` constraints actually constrain rolltemplate tables, or whether table auto-layout/min-content still controls the used width. Use `--actual-sidecar` when a fresh Roll20 chat capture was written to ignored temp output instead of the locked canonical report folder. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-min-content -- reports/roll20-actual-compare/<label> [local-smoke-json] [--actual-sidecar <fixture-id>=<json>] [--font-glyph-dir <dir>] [--intrinsic-width-dir <dir>] [--table-intrinsic-dir <dir>] [--table-layout-dir <dir>] [--source-context-dir <dir>] [--out-dir <dir>]` | Fuses text-measure deltas, table used/scroll/max-width signals, row/cell uniformity, crop/top-offset evidence, and source-context decisions into a scoped min-content route. Use this before adding another CoC/YSHY renderer candidate so broad font/width CSS is not mistaken for a Roll20-equivalent table model. Diagnostic only, not production CSS or visual parity. |
| `corepack pnpm run diagnose:roll20-chat-cell-allocation -- reports/roll20-actual-compare/<label> [local-smoke-json] [--candidate-smoke name=path] [--out-dir <writable-dir>]` | Compares actual Roll20 row/cell allocation against local or candidate ChatPane smoke reports. Use it before promoting table/cell font, wrap, width, or typography candidates; `BROAD_STYLE_BREAKS_CELL_ALLOCATION` blocks production CSS even when isolated computed styles match. Use `--out-dir` to choose a temp report folder when canonical output is locked; when no explicit `--out-dir` is supplied, the command falls back to `..\_tmp_codex_smoke\...` on `EPERM`/`EACCES`. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-overflow-crop -- reports/roll20-actual-compare/<label>` | Compares local vs actual rolltemplate table overflow, table-to-crop ratios, scroll/client width, and crop/top-offset evidence. Use after the table-intrinsic probe points at CoC/YSHY overflow/crop behavior. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-intrinsic-width -- reports/roll20-actual-compare/<label> [local-smoke-json] [--out-dir <writable-dir>] [--actual-sidecar <fixture-id>=<json>]` | Compares local and actual Roll20 rolltemplate table/row/cell intrinsic metrics, including style-proof rejection of transform/scale candidates. Use `--actual-sidecar` for fresh ignored-temp Roll20 sidecars. Use after width model flags overflowed tables such as CoC/YSHY; diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-font-glyph -- reports/roll20-actual-compare/<label> [local-smoke-json] [--out-dir <writable-dir>] [--actual-sidecar <fixture-id>=<json>]` | Compares local and actual Roll20 chat font availability, computed font stacks, broad font-candidate outcomes, row text-width signals, and exact `textMeasureEvidence` sidecars when present. It also classifies a narrow `textWidthModel` by comparing table `measureText` deltas against actual table-width deltas, so agents can split text-metric, paint/cell-allocation, and layout-constraint blockers. Use `--actual-sidecar` for fresh ignored-temp Roll20 sidecars and `--out-dir` when testing candidate smoke evidence without rewriting canonical reports. If actual sidecars predate `textMeasureEvidence.samples`, it reports recapture required instead of recommending broad font/spacing CSS. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-font-fallback -- reports/roll20-actual-compare/<label> [--fixture <fixture-id>] [--actual-sidecar <json>] [--families <css-font-list>] [--out-dir <dir>]` | Uses headless Chromium canvas measurements to compare candidate fallback font families against actual Roll20 `textMeasureEvidence` samples whose custom font family is present in computed style but not active. Use this before creating another YSHY/CoC font candidate; a close metric still needs smoke, row-raster, style proof, and renderer-gate evidence. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-font-intrinsic -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--font-glyph-dir <dir>]` | Fuses font/glyph, intrinsic-width, overflow/crop, and candidate comparison evidence. Use `--font-glyph-dir` to consume an isolated font/glyph report generated from fresh actual sidecars, and `--out-dir` for ignored temp evidence when checking the next font/intrinsic route without rewriting canonical reports. Use it when width/overflow CSS has no effect and the next hypothesis is Roll20 font-face availability/order plus table min-content sizing. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-row-paint-source -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--candidate-comparison-dir <dir>] [--style-proof-dir <dir>]` | Fuses row geometry, mask bands, paint candidate gains, actual computed styles, and Roll20 chat CSS activation/source evidence. Use override dirs when checking temp candidate evidence without rewriting canonical reports. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-row-raster -- reports/roll20-actual-compare/<label>` | Compares local and actual chat rolltemplate PNGs by DOM row, reporting row-level mismatch, luma deltas, and bright/dark mismatch shares. Use after row/paint/source routing says the next YSHY/CoC axis is raster/background/text evidence. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-row-raster-candidates -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--include-candidates <name[,name]>]` | Runs row-raster probes for selected diagnostic candidates into isolated subfolders, then compares AW2E and YSHY row-weighted and worst-row deltas against default. Use `--include-candidates` to run only the default baseline plus named candidates for faster isolated experiments; dynamic included names require matching `--candidate-smoke name=json` and `--candidate-screenshots name=dir` overrides. Use `--out-dir` when canonical generated reports are locked or when candidate comparison output should stay in a disposable temp folder. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-row-compositing -- reports/roll20-actual-compare/<label> [local-smoke-json] [local-screenshot-dir] [--out-dir <writable-dir>] [--row-raster-dir <dir>] [--row-paint-source-dir <dir>]` | Decomposes row-level PNG mismatch into edge/text, flat background, local-darker/brighter, and chroma buckets. Use override dirs after temp candidate row-raster runs to decide whether the next candidate should target text antialiasing, background compositing, color/assets, or source/capture context. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-background-source -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--row-compositing-dir <dir>] [--row-raster-candidates-dir <dir>]` | Routes background/source-context work from computed background styles, row compositing, and rejected candidate evidence. Use override dirs to carry temp candidate compositing evidence forward without contaminating canonical actual reports. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-background-raster -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--background-source-dir <dir>] [--row-compositing-dir <dir>] [--row-raster-dir <dir>]` | Routes whether current background mismatch is explained by already-tested raster models. Use override dirs after temp candidate evidence to distinguish promising luma/background probes from rejected background-size/table-scale work. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-background-source -- reports/roll20-actual-compare/<label>` | Fuses computed background style, table width/crop context, row compositing, and rejected background-size evidence. Use this when row flat-paint mismatch remains but background CSS declarations already look equivalent. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-background-raster -- reports/roll20-actual-compare/<label>` | Tests whether chat background mismatch is explained by already-tested raster-only models such as background-size/scale, row luma shift, or table width/crop context. Use after background/source routing says declarations match but pixels differ. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-background-assets -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--background-source-dir <dir>] [--background-raster-dir <dir>]` | Fetches referenced chat background image URLs and compares proxy/source bytes, hashes, image dimensions, and removed-placeholder risk. If a rerun hits transient fetch failures for unchanged URLs, it preserves stronger previous byte evidence from the same run and marks `preservedFetchFailureCount` instead of downgrading the fixture to a weaker fetch-only state. Use overrides before browser-paint tuning when candidate-specific CSS declarations match but pixels differ. Diagnostic only, not production CSS. |
| `corepack pnpm run plan:roll20-chat-assets -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--asset-probe-dir <dir>] [--background-raster-dir <dir>] [--target-plan-dir <dir>]` | Converts chat background asset/proxy evidence into product requirements. It distinguishes missing/relinked source assets from renderer CSS work and keeps evidence local-only. Use overrides when candidate-specific asset/raster evidence should flow into the same policy branch without rewriting canonical reports. |
| `corepack pnpm run plan:roll20-chat-browser-paint -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>]` | Routes browser paint/decode work after asset preservation checks. It blocks paint probes while asset relink is still required and keeps renderer CSS held until decoded bytes/background declarations/raster evidence agree. Use `--out-dir` when you intentionally want a specific report folder; when no explicit `--out-dir` is supplied, locked canonical writes fall back to `..\_tmp_codex_smoke\...` on `EPERM`/`EACCES`. |
| `corepack pnpm run plan:roll20-asset-relink -- reports/roll20-actual-compare/<label> --map-file <local-map.txt>` | Checks whether a local-only `old URL => new URL` replacement map covers the current asset-preservation blockers. It also writes an ignored `asset-relink-map-template.txt` with commented candidate URL rules for unresolved blockers. URL text only; no asset bytes are downloaded or committed. When no explicit `--out-dir` is supplied, locked canonical writes fall back to `..\_tmp_codex_smoke\...` on `EPERM`/`EACCES`. |
| `node scripts/roll20_actual_local_baseline.mjs --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/roll20-actual-compare --run-label <label> --asset-map-file <local-map.txt>` | Regenerates local preview/edit screenshots and Roll20 upload payloads with a local-only asset replacement map applied. Use only with user-owned replacement URLs; generated screenshots/payloads stay ignored. |
| `corepack pnpm run test:asset-refs` | Self-test for shared import/export asset URL classification and commented relink draft generation: external, relative, data, Roll20 proxy, Imgur page, placeholder risk, and `old URL => <paste-user-owned-https-url-here>` draft entries. |
| `corepack pnpm run test:asset-replacements` | Self-test for the export dialog's local-only asset URL replacement map. Verifies valid `old => new` parsing, warning handling, and HTML/CSS URL replacement. |
| `corepack pnpm run diagnose:roll20-chat-current-metrics -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>]` | Audits actual Roll20 `roll20-chat-dom-evidence.json` sidecars for the current row/typography/text-rasterization/filter fields required by renderer diagnostics. Use before tuning ChatPane CSS so stale sidecars are recaptured instead of treated as style proof. Use `--out-dir` when the canonical report output files are locked; when no explicit `--out-dir` is supplied, locked canonical writes fall back to `..\_tmp_codex_smoke\...` on `EPERM`/`EACCES`. Diagnostic only, not production CSS. |
| `corepack pnpm run diagnose:roll20-chat-structure -- reports/roll20-actual-compare/<label>` | Compares the local ChatPane rolltemplate class, selected roll button, row signature, and text against actual Roll20 chat sidecars. Use before pixel tuning; if it reports a template/row/text mismatch, recapture or fix rolltemplate runtime mapping before CSS. |
| `corepack pnpm run smoke:worker-state -- --out-dir ./out --base-path /roll20-block-editor --report-dir reports/sheet-worker-state-smoke` | Browser smoke for Roll20-style action button -> worker `setAttrs` -> hidden input value attribute -> CSS `[value=...]` state selector updates, plus duplicate `attr_*` checkbox mirroring for CSS `:checked` anchors. Local preview iframe only; not actual Roll20 parity. Needs `corepack pnpm run build` first. |
| `corepack pnpm run audit:worker -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/worker-source-audit` | Browser import/export audit for source `<script type="text/worker">` preservation. Writes local-only results and treats exact worker body preservation as the gate; not actual Roll20 worker runtime parity. Needs `corepack pnpm run build` first. |
| `node scripts/roll20_actual_compare_manifest.mjs [run-label]` | Create an ignored local checklist/report scaffold for Roll20 room/sandbox/test-room comparison evidence. |
| `node scripts/roll20_actual_local_baseline.mjs --out-dir ./out --report-dir reports/roll20-actual-compare --run-label <label> [--only <fixtureId>] [--state-map reports/visual-state-candidates/visual-state-candidates-state-map.json]` | Import ignored fixtures through the static app, capture local preview/edit screenshots, and write Roll20 Sandbox payload files plus `upload.zip` into ignored `reports/roll20-actual-compare/<label>/`. Optional `--state-map` applies local preview action-state hints before the preview screenshot only; exported payload and edit screenshot remain source-derived. Needs `corepack pnpm run build` first. |
| `corepack pnpm run audit:payload -- reports/roll20-actual-compare/<label>` | Audit generated Roll20 upload payloads for app UI/edit overlay/internal id leakage, JSON validity, and zip/file consistency. Local-only hygiene gate; not actual Roll20 visual parity. |
| `corepack pnpm run audit:roll20-sandbox-sanitize -- reports/roll20-actual-compare/<label>` | Apply the observed Roll20 Custom Sheet Sandbox sanitize/prefix approximation to generated payload HTML/CSS and report what Roll20 would likely rewrite, proxy, strip, or reject. Local diagnostic only; not actual Roll20 visual parity. |
| `corepack pnpm run smoke:roll20-sandbox-preview -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --fixture <fixtureId> --report-dir reports/roll20-sandbox-preview-smoke` / `corepack pnpm run smoke:roll20-sandbox-preview:all -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/roll20-sandbox-preview-smoke` | Import one ignored fixture or every prepared visual fixture through the static app, toggle the local Roll20 Sandbox expected-render preview mode, and verify that iframe HTML/CSS changes match the sanitizer approximation. Console/resource issues are reported separately as `Console status`; pass `--fail-on-console-issues` for a strict console gate. Local preview diagnostic only; not actual Roll20 visual parity. Needs `corepack pnpm run build` first. |
| `corepack pnpm run audit:state-selectors -- --fixtures test-fixtures/visual --payload-run reports/roll20-actual-compare/<label> --report-dir reports/state-selector-audit` | Audit hidden/value/checked CSS state selectors against source HTML controls and generated payload controls. Catches default-tab/default-state anchor loss; not visual parity. |
| `corepack pnpm run audit:assets -- --fixtures test-fixtures/visual --payload-run reports/roll20-actual-compare/<label> --report-dir reports/asset-resource-audit` | Scan fixture/payload HTML/CSS asset URLs and probe HTTP(S) reachability with no referrer. Catches payload-introduced missing/failed asset regressions; not visual parity. |
| `corepack pnpm run audit:assets:browser -- --fixtures test-fixtures/visual --payload-run reports/roll20-actual-compare/<label> --report-dir reports/asset-resource-audit` | Adds a Chromium image-load probe for image-like refs only. Use this when HTTP probes pass but browser resource warnings remain; it distinguishes dead image URLs from render-context/request-abort issues. Not visual parity. |
| `corepack pnpm run guard:roll20-evidence -- reports/roll20-actual-compare/<label>` | Check that private fixtures/reports/examples are not tracked/staged and that a Roll20 actual-screen run has local baseline, payload audit, and cleaned-payload roundtrip evidence before sandbox/test-room upload. This is a safety guard, not a parity claim. |
| `corepack pnpm run handoff:roll20-upload -- [reports/roll20-actual-compare/<label>] [fixture-id]` | Create an ignored local upload checklist with exact Roll20 Sandbox payload paths, screenshot destinations, and the screenshot diff command. If the run folder is omitted, the newest PASS pre-upload run is selected. Use when Chrome blocks automated file upload. |
| `corepack pnpm run snippet:roll20-upload -- [reports/roll20-actual-compare/<label>] [fixture-id] [--apply-settings --endpoint-campaign-id <id>]` | Create ignored Sandbox-only browser snippets that embed the generated payload, dispatch `change` events on Roll20 Sandbox Tools file inputs, fill the submitted `customcharsheet_json` form control with the settings-page `{ sheet, userOptions, jsoninfo }` wrapper derived from exported `sheet.json` when the settings page is open, and generate a matching `*-activation-check-snippet.js` for `/editor` after save/reload. Default snippets do not submit settings or POST the endpoint fallback. Use `--apply-settings --endpoint-campaign-id <id>` only for the dedicated Sandbox/test room when intentionally applying a generated fixture; this turns on endpoint fallback and settings save. On 2026-06-21 live Roll20 verification, a broad manifest selector wrote duplicate JSON objects into `customcharsheet_json` and made `/editor` return a JSON parse error. The snippet now avoids writing Ace text-input mirrors as submitted manifest fields. The activation check must return `VISIBLE_MATCH` before actual sheet-root screenshot capture. `SHEET_IFRAME_PRESENT_NEEDS_FRAME_PROBE` means Roll20 opened a character-sheet iframe but top-document JS could not prove its contents, `CHARACTER_DIALOG_NO_SHEET_BODY` means a character dialog is open without sheet body markers, and `CHAT_TEMPLATE_ONLY` means a rolltemplate is visible in chat but sheet body markers are not proven. |
| `corepack pnpm run preflight:roll20-cdp -- --run-dir reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--fixture <fixture-id>]` | Check the already-open CDP browser at `127.0.0.1:9222`, classify real Roll20 page targets, read the current actual status, and print the next safe capture action. Use `--out-dir` to write preflight output into a temp report folder when Windows has locked the run's existing `roll20-cdp-preflight` files. This helper never uploads sheets, edits rooms, clicks rolls, or captures screenshots by itself. |
| `corepack pnpm run apply:roll20-upload-cdp -- --run-dir reports/roll20-actual-compare/<label> --fixture <fixture-id> --endpoint-campaign-id <id> [--out-dir <ignored-temp-dir>]` | Execute the generated `snippet:roll20-upload` helper through an already-open CDP Chrome/Edge Roll20 session. It navigates to `https://app.roll20.net/sheetsandbox/settings/<id>` by default, refuses to run without an explicit sandbox/test campaign id, saves a local-only result JSON under `reports/.../roll20-upload-handoff/cdp-apply/` or `--out-dir`, and does not treat upload/storage as visual parity. After it runs, reload/open the dedicated editor, open the intended character, then run `probe:roll20-sheet-frame` before any root/chat capture. |
| `corepack pnpm run open:roll20-character-cdp -- --list` / `-- --character-id <id>` / `-- --character-name <name>` / `-- --character-id <id> --close` | Connect to the already-loaded CDP Roll20 Sandbox/test-room editor and list, open, or close a character sheet dialog through Roll20's own `Campaign.characters` view path. Use this after a successful settings/upload step and before `probe:roll20-sheet-frame` when the editor URL is ready but no character-sheet iframe is open. This only opens/closes the local viewer dialog; it does not upload source, save room settings, edit character data, click rolls, or capture screenshots. |
| `corepack pnpm run probe:roll20-sheet-frame -- --run-dir reports/roll20-actual-compare/<label> --fixture <fixture-id> [--out-dir <ignored-temp-dir>] [--cdp http://127.0.0.1:9222] [--dry-run] [--plan-only]` | Connect to an already-open CDP-enabled Roll20 Sandbox/test-room page, inspect the top page and child frames, and save `roll20-sandbox-dom-evidence.json` only when the character-sheet iframe contains strong expected payload markers: at least one expected roll button, one expected visible text token, or five expected attr markers. This avoids treating generic `attr_str`/`attr_int` overlap as fixture proof. Use `--out-dir` for fresh evidence when the canonical report folder is locked or should not be rewritten. Local DOM evidence only; not visual parity. |
| `corepack pnpm run test:roll20-upload-snippet` | Self-test the Roll20 upload and activation-check snippet builders. It fails if the settings-page manifest regresses from `{ sheet, userOptions, jsoninfo }` back to plain exported `sheet.json`, if the snippet tries to write the Ace text input as another submitted manifest field, if default snippets become submitting by accident, if explicit apply snippets do not enable endpoint/save mode, or if the generated `/editor` activation checker stops reporting `VISIBLE_MATCH`, `SHEET_IFRAME_PRESENT_NEEDS_FRAME_PROBE`, `CHARACTER_DIALOG_NO_SHEET_BODY`, `CHAT_TEMPLATE_ONLY`, `ROLL20_EDITOR_PARSE_ERROR`, and `NOT_PROVEN`. Tooling guard only; not actual Roll20 upload proof. |
| `corepack pnpm run status:roll20-actual -- [reports/roll20-actual-compare/<label>] [--require-actual] [--out-dir <writable-dir>]` | Summarize one actual-screen verification run: pre-upload gate status, payload/local screenshot readiness, missing Roll20 screenshots, existing diff status, trusted full-root count, renderer action readiness, and recorded upload blockers. With `--require-actual`, missing generated-sheet Sandbox/chat screenshots or diffs make the command fail; existing solo-room observation screenshots are reported separately. Use `--out-dir` to choose a temp report folder when Windows has locked the run's existing `actual-verification-status` files; when no explicit `--out-dir` is supplied, the command now falls back to `..\_tmp_codex_smoke\...` on `EPERM`/`EACCES`. This prevents confusing upload-readiness with visual parity or production-renderer readiness. |
| `corepack pnpm run diagnose:roll20-chat-refresh -- reports/roll20-actual-compare/<label> [--work-run-dir <empty-temp-run-dir>]` | Rerun the full Roll20 chat diagnostic chain from one evidence set. Use `--work-run-dir` when the canonical run may be locked or when you need an isolated refresh: it copies the whole run into an empty temp run folder, regenerates downstream reports there, and leaves the canonical evidence untouched. `node scripts/roll20_chat_diagnostic_refresh.mjs --self-test` checks the isolated-copy guard. Diagnostic only, not visual parity. |
| `corepack pnpm run plan:roll20-chat-capture -- reports/roll20-actual-compare/<label> [fixture-id] [--all] [--require-current-metrics] [--out-dir <ignored-temp-dir>]` | Create an ignored local handoff plan for missing or stale Roll20 chat evidence. It lists exact `roll20-chat.png` and `roll20-chat-dom-evidence.json` destinations, current stale/missing reasons, suggested source roll button names, follow-up commands, and a browser-side DOM probe snippet. `--require-current-metrics` also plans recapture when the sidecar predates current row/typography fields such as `latestTemplate.rowMetrics`, computed styles, table box metrics, font evidence, `textMeasureEvidence.samples`, and viewport DPR. Use `--all --out-dir ..\_tmp_codex_smoke\...` when canonical `reports/` output is locked but a fresh probe snippet is needed. Planning/snippet output only; not Roll20 visual parity. |
| `corepack pnpm run capture:roll20-chat-cdp -- --run-dir reports/roll20-actual-compare/<label> --fixture <fixture-id> [--out-dir <ignored-temp-dir>] [--snippet <probe-snippet.js>] [--sheet-frame-evidence <json>] --cdp http://127.0.0.1:9222 [--roll-button roll_name]` | Connect to an already-open Chrome/Edge CDP endpoint for the dedicated Roll20 Sandbox/test room, require positive sheet-frame evidence from `probe:roll20-sheet-frame`, optionally click a roll button, execute the generated or explicit chat DOM probe snippet, and save paired local-only `roll20-chat.png` plus `roll20-chat-dom-evidence.json`. Use `--snippet`, `--sheet-frame-evidence`, and `--out-dir` together when the probe plan, sheet-frame proof, and capture are intentionally isolated in ignored temp output. The capture records both DOM/CSS and top-level screenshot clips, and refuses to overwrite evidence when the PNG lacks visible foreground. This is not a login, upload, or visual-parity claim by itself. |
| `corepack pnpm run audit:roll20-chrome-observation -- reports/roll20-actual-compare/<label>/chrome-extension-roll20-observation/<fixture>[/after-refresh]` or `-- reports/roll20-actual-compare/<label>/local-baseline/<fixture>/screenshots` | Audit local-only observations captured through the Chrome extension path. It detects DOM-template-only evidence, `.png` filenames containing JPEG bytes, page screenshots without a trusted `roll20-chat.png` crop, session-refresh markers, and coordinate inconsistencies before anyone promotes extension screenshots as Roll20 chat parity proof. Diagnostic only; use CDP capture or a verified full-screenshot crop adapter for trusted chat evidence. |
| `corepack pnpm run test:roll20-chat-capture-plan` | Self-test the generated Roll20 chat DOM probe snippet shape. It verifies the snippet can produce JSON-serializable `clip`, `screenshotClipApplied`, `screenshotCssClip`, `rolltemplates[].rect`, `chatCssEvidence`, and `textMeasureEvidence` in a fake Roll20 chat DOM without reusing clip alias objects. This only validates the capture tool; it is not actual Roll20 evidence. |
| `corepack pnpm run diagnose:roll20-chat-live-typography -- reports/roll20-actual-compare/<label> [probe.json] [local-smoke.json] [--expect-fixture fixture-id]` | Compare a read-only live Roll20 typography probe against local chat smoke metrics and map the selected rolltemplate class to the matching fixture. `--expect-fixture` exits nonzero when the live card is for a different fixture, which prevents a probe from being misread as YSHY/AW2E/Les evidence. DOM/style evidence only; not visual parity. |
| `corepack pnpm run diagnose:roll20-chat-candidate-style -- reports/roll20-actual-compare/<label>` | Compare promising local ChatPane candidates against actual Roll20 computed styles. It now covers `candidate-needs-style-proof` and `single-fixture-only` candidates such as `coc-table-scale-x`, so pixel-improving but style-contradicted hacks stay out of product defaults. Diagnostic only; not visual parity. |
| `corepack pnpm run diagnose:roll20-chat-residual -- reports/roll20-actual-compare/<label>` | Classify remaining local ChatPane vs actual Roll20 chat mismatch into residual axes such as shadow/border rasterization or geometry width conflict. Diagnostic-only; run this before trying another ChatPane CSS candidate. |
| `corepack pnpm run gate:roll20-renderer-ready -- reports/roll20-actual-compare/<label>` | Fail unless `status:roll20-actual` sees generated actual evidence plus trusted full-root coverage and a non-HOLD renderer action. Use this before production renderer CSS work or any claim that Roll20 visual parity is ready for implementation. |
| `corepack pnpm run crop:roll20-actual -- --image <viewport.png> --meta <meta.json> --out <roll20-sandbox-root.png> [--rect iframeRect] [--inset-css left,top,right,bottom]` | Crop a Roll20 viewport screenshot using measured dialog/iframe metadata. Use this when the Roll20 iframe document cannot be read but the visible sheet area can be normalized from a full viewport screenshot. Writes local-only crop evidence; not visual parity. |
| `corepack pnpm run plan:roll20-root-capture -- reports/roll20-actual-compare/<label> [fixture-id]` | Create an ignored local handoff plan for fixtures missing trusted DPR-corrected full-root evidence. The report lists missing required files, existing diagnostic-only captures, trusted capture examples from the same run, follow-up commands, and a browser metrics snippet. It is planning evidence only; not visual parity. |
| `corepack pnpm run stitch:roll20-actual-root -- --manifest <roll20-root-stitch-manifest.json> [--out <roll20-sandbox-root-full-dpr-corrected.png>]` | Stitch multiple local-only Roll20 root viewport segments into a full-height sheet-root image. Prefer `roll20-sandbox-root-full-dpr-corrected.png` when the source segments were captured with DPR-corrected Chrome clips; `roll20-sandbox-root-full.png` is the legacy fallback. Segment coordinates may crop from a full viewport screenshot, or use `"cropImage": "full"` when Chrome already saved a clipped root segment. The stitcher records segment hashes, and `audit:roll20-root-stitch` fails trusted evidence if repeated byte-identical segments are present. Diff/status helpers prefer DPR-corrected full-height evidence over the legacy full-root file and visible root crop. Local evidence only; not visual parity. |
| `corepack pnpm run stitch:roll20-overlap-diagnostic -- --out <diagnostic.png> <segment-000> <segment-001> [...]` | Visually stitch scrolled Roll20 viewport/root segment screenshots by overlap when iframe DOM/scrollTop cannot be read. This is only a local diagnostic to inspect whether segment captures are coherent; visible seams or weak overlap must not be promoted to trusted `roll20-sandbox-root-full-dpr-corrected.png` evidence. |
| `corepack pnpm run audit:roll20-root-stitch -- reports/roll20-actual-compare/<label>` | Audit stitched Roll20 full-root/root capture metadata before treating it as trusted full-root evidence. It also lists overlap-stitch outputs as `DIAGNOSTIC_ONLY` so they are not confused with trusted DPR-corrected root captures. Local evidence only; not visual parity. |
| `corepack pnpm run classify:roll20-actual -- reports/roll20-actual-compare/<label>` | Classify actual Roll20 screenshot differences using the local baseline, screenshot diff, sandbox sanitize audit, status report, and optional chat DOM evidence. Writes ignored `actual-difference-classification/` output. Heuristic triage only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-visible-crop -- reports/roll20-actual-compare/<label>` | Diagnose a matched visible Roll20 root-crop mismatch with local-only PNG artifacts: local visible crop, best top-aligned local crop, normalized Roll20 crop, and diff overlay. Reports mismatch bands/quadrants and whether simple horizontal crop drift explains the mismatch. Diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-visible-context -- reports/roll20-actual-compare/<label>` | Summarize the local/actual context gaps behind a Roll20 root-crop mismatch: local preview size/state, actual iframe/crop metadata, sandbox sanitize rewrites, crop-gain evidence, chat DOM vs screenshot evidence, and ranked next probes. Use this when the Roll20 iframe DOM/CSS is not readable. Diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-geometry -- reports/roll20-actual-compare/<label>` | Compare actual Roll20 iframe computed geometry from `live-iframe-probe/` against the best local candidate evidence. It prefers `*-target-geometry-deep.json` when present, uses `smoke:roll20-same-context-visible` when comparable, and falls back to `smoke:roll20-full-root-candidates` when full-height stitched root evidence is the current source of truth. It ranks selector/source parity, root height delta, row/table/input/button geometry deltas, row child relative-y deltas, repeating-control/table row deltas, unresolved deep-probe gaps, and next checks. Local diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-root-cutoff -- reports/roll20-actual-compare/<label> [fixture-id]` | Compare trusted stitched Roll20 root evidence against live iframe sidecar root metrics. Use this when stitched root height and live sidecar root height disagree. Cutoff/capture diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-state-visibility -- reports/roll20-actual-compare/<label>` | Compare actual Roll20 hidden state inputs, visible panels, CSSOM state rules, and generated payload class/selector shapes. Use this when local preview state behavior differs from actual Roll20. Diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-visibility-selectors -- reports/roll20-actual-compare/<label> [fixture-id]` | Audit emitted payload HTML/CSS for hide selectors that only match through a `sheet-` alias or do not match at all. Use this before changing Roll20 prefix/sanitize behavior. Source-level diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-height-drift -- reports/roll20-actual-compare/<label> [fixture-id]` | Compare the preferred full-root local candidate against actual Roll20 stitched root evidence and classify large vertical drift, including local extra visible content and over-hidden local candidates. Image diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-playbook-state -- reports/roll20-actual-compare/<label> [fixture-id]` | Summarize playbook/default-state candidates from the full-root smoke report, separating pixel-best from height-closest candidates. Use this when a sheet's default/checked state appears to bracket actual Roll20 height. Derived diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-attr-class-visibility -- reports/roll20-actual-compare/<label> [fixture-id]` | Compare actual Roll20 `attr_class` sidecars with emitted payload HTML/CSS selector shapes. Use this when actual checked values do not explain visible panels. Selector/state diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-attr-class-geometry -- reports/roll20-actual-compare/<label> [fixture-id]` | Compare actual Roll20 `attr_class` panel rects against stitched full-root height and emitted source order. Use this when actual-visible panel names do not reproduce full-root height. Geometry diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run plan:roll20-attr-class-state -- reports/roll20-actual-compare/<label> [fixture-id]` | Generate an ignored local capture plan and browser snippet for actual Roll20 `attr_class`/default-state probing. Use this when full-root candidates bracket the actual height and a browser-side checked/value state sidecar is needed before renderer CSS changes. Planning/snippet output only; not Roll20 visual parity. |
| `corepack pnpm run gate:roll20-renderer-action -- reports/roll20-actual-compare/<label> [--out-dir <writable-dir>] [--chat-candidate-comparison-dir <report-dir>] [--chat-candidate-style-proof-dir <report-dir>] [--chat-row-raster-candidates-dir <report-dir>] [--row-paint-source-dir <report-dir>] [--chat-source-context-dir <report-dir>] [--chat-table-budget-dir <report-dir>] [--cell-allocation-dir <report-dir>] [--chat-template-scope-dir <report-dir>]` | Consolidate actual status, full-root candidates, state visibility, geometry, chat candidate/style/row-raster evidence, chat table-budget, chat source/context, chat row/paint/source, chat template scope, and cell-allocation diagnostics into a renderer action recommendation such as `HOLD_PRODUCTION_RENDERER_PATCH`. Use before promoting diagnostic CSS candidates into production renderer code. Use `--out-dir` and report overrides when canonical output files are locked; when no explicit `--out-dir` is supplied, the command now falls back to `..\_tmp_codex_smoke\...` on `EPERM`/`EACCES`. If the canonical cell-allocation report is missing, the gate also auto-uses the newest matching `..\_tmp_codex_smoke\chat-cell-allocation-probe-<run>*` report whose JSON `runDir` matches the active run. Diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run diagnose:roll20-computed-style-context -- reports/roll20-actual-compare/<label>` | Compare actual Roll20 computed-style sidecars from `live-iframe-probe/<fixture>-computed-styles.json` against local full-root candidates for row/column/table/input/textarea context. Use before promoting inline-flow, input-height, textarea, or wrapper/base CSS candidates. Local diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run smoke:roll20-same-context-visible -- reports/roll20-actual-compare/<label>` | Render the generated payload locally in several Roll20-like visible contexts, including sandbox-sanitize, no-state, measured frame/inset, measured DPR, actual-root-width, actual-css-scale, and dialog-padding candidates, then compare those candidate crops against the existing actual Roll20 root crop. If `live-iframe-probe/<fixture>-computed-styles.json` exists, the report also compares actual Roll20 computed styles against the best local candidate, uses style score to break near-ties, reports native-pixel mismatch, and normalizes selector sample coordinates relative to the sheet root. Computed-style selector rows include source labels such as `selected`, `missing`, or `visibleTop-fallback (partial)`; do not treat partial fallback counts as exact Roll20 selector counts. This narrows whether a mismatch is state/context/layout versus renderer/CSS. Local diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run smoke:roll20-full-root-candidates -- reports/roll20-actual-compare/<label>` | Compare local full-root render candidates against the preferred stitched actual evidence: `roll20-sandbox-root-full-dpr-corrected.png` first, then legacy `roll20-sandbox-root-full.png`. It separates source/default state, state-map, local Sandbox expected sanitize, and actual-root-width candidates, then reports row/table/image geometry deltas against live iframe probes when available. Local diagnostic only; not Roll20 visual parity. |
| `corepack pnpm run verify:roll20-preupload -- reports/roll20-actual-compare/<label> --fixtures test-fixtures/visual --out-dir ./out --base-path /roll20-block-editor [--state-map reports/visual-state-candidates/visual-state-candidates-state-map.json]` | Run the full local pre-upload gate: regenerate the local baseline/upload payloads for the run, then run payload audit, Roll20 sandbox sanitize audit, cleaned-payload visual roundtrip, state selector audit, asset audit, and evidence guard. Optional `--state-map` is forwarded to baseline and payload screenshots. Passing means ready to upload, not Roll20 visual parity. |
| `corepack pnpm run audit:legacy-export -- --report-dir reports/legacy-export-audit` | Audit the synthetic legacy export path: modern CSS remains untouched, legacy CSS runs through `sanitizeForRoll20Legacy`, and `ExportDialog` only writes `sanitize-warnings.json` in legacy mode. Local-only sanitizer/export routing gate; not actual Roll20 legacy visual parity. |
| `corepack pnpm run smoke:legacy-preview -- --report-dir reports/legacy-preview-smoke` | Smoke the local preview/edit legacy CSS toggle through `buildSheetDoc` and `buildSheetParts` with synthetic CSS. Confirms iframe and Shadow/edit render paths consume `legacyCssSanitize`; not actual Roll20 legacy visual parity. |
| `corepack pnpm run smoke:legacy-fixture-visual -- --out-dir ./out --base-path /roll20-block-editor --fixtures test-fixtures/visual --report-dir reports/legacy-fixture-visual` | Import ignored fixtures into the static app, capture preview iframe screenshots with legacy CSS sanitize off/on, and verify that legacy-risk CSS is reduced when applicable. Local-only visual smoke; not actual Roll20 legacy visual parity. |
| `corepack pnpm run smoke:export-dialog -- --out-dir ./out --base-path /roll20-block-editor --report-dir reports/export-dialog-smoke` | Static-app browser smoke for the core empty shell, header import/export actions, and the Roll20 export readiness panel. Confirms readable Korean header/empty-state copy, no sample UI when the public catalog is empty, no mojibake in the initial shell text, export readiness items, the actual-verification badge, import dialog opening, and main mode tab clicks. |
| `corepack pnpm run smoke:payload-roundtrip -- reports/roll20-actual-compare/<label> --out-dir ./out --base-path /roll20-block-editor [--state-map reports/visual-state-candidates/visual-state-candidates-state-map.json]` | Re-import cleaned Roll20 payload files through the static app and compare payload preview screenshots against the local baseline screenshots. Optional `--state-map` applies the same local preview action-state hint before payload screenshot capture. Local-only pre-upload visual gate; not actual Roll20 visual parity. |
| `node scripts/roll20_actual_screenshot_diff.mjs reports/roll20-actual-compare/<label>` | Compare local baseline screenshots against local-only Roll20 screenshots named `roll20-sandbox.png`, `roll20-room.png`, or `roll20-chat.png`. The helper loads PNG/JPEG screenshots as data URLs so Windows and Korean-path report folders work reliably. Missing Roll20 screenshots are SKIP, not PASS. |
| `node scripts/serve_static_dir.mjs <root_dir> [port]` | Serve workspace-owned reports/fixtures over localhost for browser verification. |
| `node scripts/run_selected_roundtrip.mjs` | Run selected Node-side roundtrip checks. |
| `corepack pnpm run smoke:edit-flow -- --out-dir ./out --report-dir reports/edit-flow-smoke` | Headless-browser smoke for the flow drop slice: perf-hook append paths plus real dragover/drop DragEvents on the edit canvas. Needs `corepack pnpm run build` first and a playwright chromium-headless-shell install. |
| `node scripts/browser_roundtrip_smoke.mjs --out-dir ./out --report-dir reports/roundtrip-browser [--only <fixtureId>]` | Browser L2 roundtrip: importSheet -> emit -> re-import -> compare (block ids stripped) over `test-fixtures/visual/*`. Same build/browser prerequisites as the edit-flow smoke. |

## Output Rules

- Scripts should write local evidence to ignored `reports/<pipeline>/`.
- Temporary build files should go under `.tmp/`.
- Copied external sheet files should go under ignored `test-fixtures/`.
- Do not commit generated reports, fixture HTML, source screenshots, or copied sheet assets.
- Scripts must not mutate external source folders.
